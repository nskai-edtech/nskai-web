# Bootcamp poster

One image, in the Claude Design project
`6a001235-c66a-47ec-867c-58727c83dccb` as `uploads/IMG_2396.PNG`. Export it and
save here as `poster.png`.

It is the announcement poster — "Intro to AI Agents: from RAG to deployment" —
and renders at its natural aspect ratio. The page builds without it.
